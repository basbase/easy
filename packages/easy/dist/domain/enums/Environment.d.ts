import { Enum } from '../../types';
export declare class Environment extends Enum {
    static readonly Dev: Environment;
    static readonly Tst: Environment;
    static readonly Acc: Environment;
    static readonly Prd: Environment;
}
